# SB-M36-002 — Catalog validation against Difficulty V1

Status: IMPLEMENTED
Requirement: M35 production catalog must validate against Difficulty V1
compatibility — known class token, dimensions within engine envelope, class not
derived from dimensions, TEST fixtures rejected.

## Production owner
`scripts/difficulty/difficulty_v1_catalog_check.gd::validate_catalog(catalog)`:
- known class token (EASY/MEDIUM/HARD/VERY_HARD);
- width/height within envelope from `level_progression_v1.json`
  (`productionBoardEnvelope`, 20..59);
- explicitly does NOT assert a class->dimension relationship;
- TEST rejection delegated to the catalog (`SB-M35-010`), which fails closed
  before an entry ever reaches this checker.

## Direct tests
`tests/m36_difficulty_v1.gd::_catalog_compatibility` — M35 production catalog
validates with zero errors.

## Failure condition
Unknown class token or out-of-envelope dimension accepted.

## Actual
PASS.
