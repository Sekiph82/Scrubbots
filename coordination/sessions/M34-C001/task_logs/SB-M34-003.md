# SB-M34-003 — Completion haptic

Status: IMPLEMENTED
Requirement: observe M30 terminal latch; fire only for WON, at most once per
attempt. LOST/ERROR play nothing.

## Production owner
`HapticsController._on_terminal_reached(status, detail)` +
`_completion_played_this_attempt` latch, mirroring M33 audio.

## Direct tests
`tests/m34_haptics_runtime.gd::_completion_latch`:
- LOST/ERROR: played == 0.
- WON: latches; repeated WON does not duplicate.
- `reset_for_new_attempt()` re-arms; fresh WON plays again (played == 2).
- Platform sink observed exactly 2 completion vibrations with the
  `COMPLETION_MS` duration each.

## Expected
Exactly one platform call per attempt on WON.

## Failure condition
Any LOST/ERROR call to platform, or duplicate WON call.

## Actual
PASS.
