# SB-M34-004 — Toggle

Status: IMPLEMENTED
Requirement: runtime enable/disable seam with documented default. OFF must
suppress requests without mutating gameplay. Persistence UI belongs to M41.

## Production owner
- Runtime seam: `HapticsController.set_enabled(bool)` / `is_enabled()`.
- Persistence seam: `HapticsSettingsService` (isolated ConfigFile at
  `user://haptics_settings.cfg`) with `snapshot()` / `import_snapshot()` for M40.
- Default: enabled=true.

## Direct tests
`tests/m34_haptics_runtime.gd`:
- default enabled=true asserted;
- OFF: 20 cleaning requests -> 0 played, 20 suppressed, platform sink empty;
- OFF: completion request suppressed;
- settings save/load round-trip through isolated `user://` path;
- missing file -> safe default enabled=true.

## Expected
No platform call while OFF; gameplay clear/completion still proceed normally.

## Failure condition
Any platform call observed while OFF.

## Actual
PASS. No M41 UI added.
