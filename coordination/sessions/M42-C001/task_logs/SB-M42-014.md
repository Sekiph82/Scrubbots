# SB-M42-014 — Home asset generation — Claude Task Log

Status: CODE_COMPLETE / SB-M42-014 / OWNER_ASSET_APPROVAL_REQUIRED
Implementation SHA(s): `24c123f`
Prompt: `coordination/sessions/M42-C001/task_prompts/SB-M42-014.md`; criteria `coordination/sessions/M42-C001/audit_criteria/SB-M42-014.md`

## Changed files

- `tests/m42_assets.gd (inventory case; no images generated)`

## Tests

Command form: `godot --headless --path . -s res://tests/<name>.gd`, run in a clean detached worktree of the implementation SHA. Each run checked for exit code, footer, `FAIL:` lines and `SCRIPT ERROR` count.

| Suite | Exit | SCRIPT ERROR | FAIL lines | Footer |
|---|---|---|---|---|
| m42_assets | 0 | 0 | 0 | M42 assets cases completed: 3/3 M42 assets evidence: PASS |

Worktree SHA: `24c123f`.

`git diff --check`: clean (checked on the implementation commit).

## Criteria map

| Criterion | Evidence |
|---|---|
| Inspect existing assets first | all 49 generation_required ART targets already exist on disk (Codex batch 47b4343/3012ebb/03109d2) (`generation_inventory`) |
| Generate only what is required | 0 missing -> no generation performed, nothing overwritten or promoted |
| Provider order | ChatGPT image generation primary / Magnific fallback enforced by the 013 validator; no provider was needed |
| No self-promotion, no baked live text | none APPROVED; LIVE entries carry no image path (validator) |

## Notes

- The existing targets were committed by the owner under assets/ui/final/ from the Codex visual batch, but no owner approval record exists and the manifest status is PLANNED for all of them; they are treated as unapproved candidates.

## Remaining gates

- OWNER_ASSET_APPROVAL_REQUIRED: owner must mark approved entries APPROVED with approved_sha256 in HOME_ASSET_MANIFEST.json.

Root `TASKS.md`, ChatGPT audit files and owner decision/acceptance files untouched. No audit verdict created.

Handoff: `CODE_COMPLETE / SB-M42-014 / OWNER_ASSET_APPROVAL_REQUIRED`
