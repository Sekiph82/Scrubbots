# SB-M42-026 — Opening cinematic source — Claude Task Log

Status: AWAITING_AUDIT / SB-M42-026
Implementation SHA(s): `295fb53`
Prompt: `coordination/sessions/M42-C001/task_prompts/SB-M42-026.md`; criteria `coordination/sessions/M42-C001/audit_criteria/SB-M42-026.md`

## Changed files

- `assets/brand/opening/final_15_seconds_opening_video.mp4 (new, byte copy)`
- `assets/brand/opening/PROVENANCE.md (new)`
- `tests/m42_opening.gd (new)`

## Tests

Command form: `godot --headless --path . -s res://tests/<name>.gd`, run in a clean detached worktree of the implementation SHA. Each run checked for exit code, footer, `FAIL:` lines and `SCRIPT ERROR` count.

| Suite | Exit | SCRIPT ERROR | engine ERROR (excl. exit leak noise) | FAIL lines | Footer |
|---|---|---|---|---|---|
| m42_opening | 0 | 0 | 0 | 0 | M42 opening cases completed: 1/1 M42 opening evidence: PASS |

Worktree SHA: `295fb53`.

`git diff --check`: clean (checked on the implementation commit).

## Criteria map

| Criterion | Evidence |
|---|---|
| Owner MP4 preserved at canonical path from the local source | byte copy of `assets/brand/opening/final 15 seconds opening video.mp4` (cp -p, cmp identical); committed blob SHA verified on origin |
| Bytes/provenance, SHA-256, duration, dimensions, audio recorded | SHA-256 c044841d...4388, 25,234,301 B, H.264 High 1280x720 30 fps 15.000 s, AAC LC 44.1 kHz stereo 15.023 s (PROVENANCE.md, `mp4_preserved`) |
| Source not modified during conversion | re-checked after SB-M42-027 (`ogv_runtime_asset`) |

## Notes

- The local source lived under assets/brand/opening/ (TASKS.md text mentions assets/brand/); same file name. The spaced-name source stays untracked and untouched.

## Remaining gates

- None code-side.

Root `TASKS.md`, ChatGPT audit files and owner decision/acceptance files untouched. No audit verdict created.

Handoff: `AWAITING_AUDIT / SB-M42-026`
