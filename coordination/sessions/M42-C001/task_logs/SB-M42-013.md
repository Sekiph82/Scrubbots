# SB-M42-013 — Home asset manifest validation — Claude Task Log

Status: AWAITING_AUDIT / SB-M42-013
Implementation SHA(s): `38e1984`
Prompt: `coordination/sessions/M42-C001/task_prompts/SB-M42-013.md`; criteria `coordination/sessions/M42-C001/audit_criteria/SB-M42-013.md`

## Changed files

- `scripts/tools/home_asset_manifest_validator.gd (new)`
- `tests/m42_assets.gd (new)`

## Tests

Command form: `godot --headless --path . -s res://tests/<name>.gd`, run in a clean detached worktree of the implementation SHA. Each run checked for exit code, footer, `FAIL:` lines and `SCRIPT ERROR` count.

| Suite | Exit | SCRIPT ERROR | FAIL lines | Footer |
|---|---|---|---|---|
| m42_assets | 0 | 0 | 0 | M42 assets cases completed: 2/2 M42 assets evidence: PASS |
| m42_home | 0 | 0 | 0 | M42 home cases completed: 9/9 M42 home evidence: PASS |

Worktree SHA: `38e1984`.

`git diff --check`: clean (checked on the implementation commit).

## Criteria map

| Criterion | Evidence |
|---|---|
| IDs, paths, kinds, provider order, statuses | validator rules; 19 adversarial mutations rejected for the intended reason (`manifest_adversarial`) |
| Economy V1 semantics / banned Star, Event, Profile-XP (and coin) | token + compound checks; star-shaped FX reported as warning only |
| No duplicate final paths | only declared reuse HOME-087 -> HOME-042 allowed |
| No overwrite of APPROVED assets | APPROVED requires approved_sha256 matching the file on disk |
| Real manifest | validates, 0 approved (`manifest_valid`) |

## Notes

- Tooling/test only: validator is not part of the shipping runtime path except via HomeArtBinder (016).

## Remaining gates

- None code-side.

Root `TASKS.md`, ChatGPT audit files and owner decision/acceptance files untouched. No audit verdict created.

Handoff: `AWAITING_AUDIT / SB-M42-013`
