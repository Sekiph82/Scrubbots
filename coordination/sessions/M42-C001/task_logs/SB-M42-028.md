# SB-M42-028 — Opening VideoStreamPlayer scene — Claude Task Log

Status: AWAITING_AUDIT / SB-M42-028
Implementation SHA(s): `58282fa`
Prompt: `coordination/sessions/M42-C001/task_prompts/SB-M42-028.md`; criteria `coordination/sessions/M42-C001/audit_criteria/SB-M42-028.md`

## Changed files

- `scripts/app/opening_screen.gd (new)`
- `scenes/app/opening_screen.tscn (new)`
- `tests/m42_opening.gd`

## Tests

Command form: `godot --headless --path . -s res://tests/<name>.gd`, run in a clean detached worktree of the implementation SHA. Each run checked for exit code, footer, `FAIL:` lines and `SCRIPT ERROR` count.

| Suite | Exit | SCRIPT ERROR | engine ERROR (excl. exit leak noise) | FAIL lines | Footer |
|---|---|---|---|---|---|
| m42_opening | 0 | 0 | 0 | 0 | M42 opening cases completed: 4/4 M42 opening evidence: PASS |

Worktree SHA: `58282fa`.
- Root suite covered at production SHA 9596dd3 (SB-M42-029, contains 58282fa).

`git diff --check`: clean (checked on the implementation commit).

## Criteria map

| Criterion | Evidence |
|---|---|
| Dedicated VideoStreamPlayer opening scene using runtime OGV (not MP4) | OpeningScreen/opening_screen.tscn plays VideoStreamTheora `.ogv` (`opening_player_aspect`) |
| Portrait-safe aspect-preserving, no distortion, safe background | letterbox fit inside viewport, 16:9 preserved, centered, on black background; 5 viewports incl. tablet |
| Deterministic lifecycle cleanup | begin once; exactly one completed/failed; cleanup stops + releases stream (also on exit); watchdog 20 s (`opening_lifecycle`) |
| Isolated from gameplay/save/economy | no AppState/economy/save/progression references in code (comments excluded) |

## Remaining gates

- None code-side.

Root `TASKS.md`, ChatGPT audit files and owner decision/acceptance files untouched. No audit verdict created.

Handoff: `AWAITING_AUDIT / SB-M42-028`
