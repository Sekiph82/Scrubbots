# SB-M42-030 — Opening cinematic skip behavior (DESIGN GATE) — Claude Task Log

Status: OWNER_DECISION_REQUIRED / SB-M42-030
Implementation SHA(s): `none (no implementation by design)`
Prompt: `coordination/sessions/M42-C001/task_prompts/SB-M42-030.md`; criteria `coordination/sessions/M42-C001/audit_criteria/SB-M42-030.md`

## Changed files

- `(none) — no production skip policy chosen`

## Tests

Command form: `godot --headless --path . -s res://tests/<name>.gd`, run in a clean detached worktree of the implementation SHA. Each run checked for exit code, footer, `FAIL:` lines and `SCRIPT ERROR` count.

- No code changed for this task; no tests to run. The opening flow (SB-M42-028/029) exposes NO skip control.

`git diff --check`: clean (checked on the implementation commit).

## Criteria map

| Criterion | Evidence |
|---|---|
| Look for an explicit owner decision | searched coordination/OWNER_* and root TASKS.md at 281ea38+: only OWNER_M42_OPENING_PLAYBACK_FREQUENCY_DECISION_V01.md exists (frequency, not skip policy); TASKS.md SB-M42-030 remains [DESIGN GATE] with a *recommended* (not decided) tap/Skip |
| If none: no production skip-policy choice, no speculative implementation | OpeningScreen has no skip input; boot waits for completion or failure only |
| Publish OWNER_DECISION_REQUIRED with the exact missing decision | see Notes |

## Notes

- Exact decision needed from the owner: (1) is the V1 opening skippable at all; (2) if yes, by what input (tap anywhere / dedicated SKIP button / back button); (3) is skip available immediately or only after N seconds, and on the first-ever launch too; (4) should the skip affordance be visible (button) or hidden (tap); (5) does skipping share the SB-M42-029 outcome path into Home (recommended: yes, as a third outcome 'skipped' through the same exactly-once OPENING -> HOME edge, no save/game-state effect).
- Once decided, the implementation is a narrow addition to OpeningScreen (input -> _finish(true, 'skipped')) plus transition/idempotency tests; SB-M42-031 frequency already treats completion/skip identically.

## Remaining gates

- OWNER_DECISION_REQUIRED: opening skip policy (see Notes).

Root `TASKS.md`, ChatGPT audit files and owner decision/acceptance files untouched. No audit verdict created.

Handoff: `OWNER_DECISION_REQUIRED / SB-M42-030`
