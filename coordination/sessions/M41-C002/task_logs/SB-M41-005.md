# SB-M41-005 — Reduced Effects

Status: IMPLEMENTED (AWAITING_AUDIT)
Implementation SHA: `380754c` (+ test-only `2851a7e`, see CLAUDE_LOG_V01 §5)
Prompt: `coordination/sessions/M41-C002/CHATGPT_PROMPT_V01.md`; criteria `CHATGPT_AUDIT_CRITERIA_V01.md`

## Requirement map

| # | Requirement | Implementation | Evidence (`tests/m41_settings.gd`, 17-case ledger) |
|---|-------------|----------------|------------------------------------------------------|
| 1 | one canonical durable bool, default OFF, M40 graph, no sidecar | `scripts/settings/effects_settings_service.gd` (bool + `changed`); owned by `AppState.effects`; persisted by `SaveService.collect()` as `settings.effects = {"reduced": bool}` | `reduced_effects_persistence` (new state OFF; save JSON carries exact bool); `no_legacy_side_files` |
| 2 | strict validation, whole-candidate policy | `SaveService.validate_candidate`: `settings.effects` optional; present => must be `{reduced: exact bool}` else `effects_malformed`; `_apply` + exact-restore via `_apply_effects` (absent => OFF) | 7/7 non-bool values and 5/5 malformed shapes rejected; exact `true`/`false` valid; corrupt primary rejected wholesale (defaults, no partial audio apply). Probe `lenient` -> 3 FAIL |
| 3 | AppState action/read, blocked refusal, canonical save | `AppState.set_reduced_effects(on)` -> `request_save()`; `{ok:false, app_blocked}` when blocked; read `app_state.effects.is_reduced()` | `blocked_app_readonly` (refused, state unchanged, save bytes unchanged, panel toggle disabled) |
| 4 | live production binding, no rebuild, FX enable independent | `ProductionGameplayHost.build()`: `_cleaning_fx.set_reduced_effects(app_state.effects.is_reduced())` + `effects.changed -> _cleaning_fx.set_reduced_effects` (accepted M31 seam; M31 controller unchanged) | `reduced_effects_live_host`: same controller instance flips ON/OFF live; FX-disabled stays disabled; host built while ON starts reduced. Probes `nolivebind` -> 6 FAIL, `nobuildconsume` -> 2 FAIL |
| 5 | one REDUCED EFFECTS toggle, re-sync, read-only when blocked, viewport matrix | `scripts/ui/settings_panel.gd`: `ReducedEffectsToggle` CheckButton after VIBRATION; `_sync_from_state` reads canonical; handler calls AppState only | `reduced_effects_ui` (default OFF, >= 88 px, font >= 30, ON/OFF persist immediately, reopen re-sync, other settings untouched); `settings_ui_controls` (exactly one control); `settings_ui_layout` (1080x2160, 1080x1920, 1080x2400, 1440x1920: panel inside 16 px gutter, all controls >= 88 px, fonts >= 30) |
| 6 | ON/OFF survive relaunch; pre-C002 save loads OFF | load path | `reduced_effects_persistence` (ON relaunch, OFF relaunch, pre-C002 primary => OFF). Probe `noapply` -> 2 FAIL |
| 7 | gameplay invariance ON vs OFF | presentation-only binding | `reduced_effects_gameplay_invariance`: OFF / ON / live-toggle-every-7-ticks runs on the real host (fixed clock + local day): identical WON, state, 400 clears, every cell state, supply exhausted, live assignments, progression and economy snapshots |
| 8 | real live presentation seam | real host `get_cleaning_fx()` | OFF: 2-sprite cue, alive at 0.20 s (0.30 s lifetime), cap 24; ON: 1-sprite cue, expired by 0.20 s (0.18 s), cap 8; back to OFF: 2 sprites |
| 9 | regressions | — | CLAUDE_LOG_V01 §4 |
| 10 | preserve scope | M31 controller, audio, haptics untouched; no M42 | CLAUDE_LOG_V01 §2 |
