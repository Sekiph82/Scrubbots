# SB-M33-001 / SB-M33-003 — Music bus + looping background music controller

Status: IMPLEMENTED (AWAITING_AUDIT) / OWNER_MUSIC_SELECTION_REQUIRED. Implementation SHA: `e4ccd7f`

- New `scripts/audio/music_controller.gd`: one AudioStreamPlayer on `Music` bus; track duplicated and set to loop (WAV loop_mode FORWARD / `loop` property), source resource not mutated; `start()` idempotent; `stop()` explicit lifecycle; stops on `_exit_tree`; fallback replay on `finished`.
- Owned by ProductionGameplayHost (gameplay scene lifecycle); never touched by dispatch/clear/terminal/Retry/speed.
- `APPROVED_TRACK_PATH = res://assets/audio/music/background_loop.ogg` does NOT exist -> status `OWNER_MUSIC_SELECTION_REQUIRED`; no music invented/downloaded. Owner exports the approved track to that path (or a follow-up changes the path).
- Music volume/mute comes from the canonical AudioSettingsService (Music bus) — see M41-C001 V01.

Evidence: `music_controller_unit` (Music bus, OWNER_MUSIC_SELECTION_REQUIRED, start refuses without track, looping, source not mutated, start count 1 after repeated start, stop, restart, leave-tree stops); `real_stack_1x/2x` (injected silent test loop keeps playing through the whole drain + Retry + second drain with start count 1; scene exit stops it). Probe `musicrestart` (restart music on Retry) -> 2 FAILs.
