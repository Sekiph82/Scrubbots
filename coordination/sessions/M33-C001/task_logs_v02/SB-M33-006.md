# SB-M33-006 — Cleaning SFX (V02: dispatch.wav, short-bounded)

Status: IMPLEMENTED (AWAITING_AUDIT) — owner decision V02 §2.
Implementation SHA: `e4ccd7f`

## Change
- `CLEANING_STREAM = res://assets/audio/sfx/dispatch.wav` (0.48 s). `cleaning.wav` (1.86 s) preserved on disk, no longer referenced by runtime.
- Tail bound: each cleaning voice is aged by `advance_voices(delta)` (driven from `_process`), faded linearly over the last `CLEANING_FADE_SEC = 0.06` and hard-stopped at `CLEANING_MAX_SEC = 0.24 s`. 0.24 s <= M32 retire echo `LIFETIME 0.28` and M31 cleaning FX `NORMAL_LIFETIME 0.30`, so no voice outlives the visual disappearance cue of the cleared pixel.
- `CLEANING_VOLUME_DB = -3.0` base level (tunable at owner F6).
- Still triggered only by committed `authenticated_clear`; clear never delayed; no pre-clear seam added.

## Direct evidence
- `controller_unit`: LIVE cleaning players' `stream.resource_path == dispatch.wav`; cleaning.wav unreferenced.
- `cleaning_tail_bound`: voice playing before bound; fade observed; player stopped + released after bound; max age <= 0.24; cut == 1; volume restored for reuse; NaN/INF/negative delta ignored; completion unbounded.
- `real_stack_1x/2x`: with interleaved 60 fps ageing, no cleaning voice aged > 0.24 s (max 0.233) and the bound cut voices in the real flow (1x cut 30, 2x cut 21).
- Probes: `tailbound` (bound 5.0) -> 4 FAILs; `oldcleaningwav` (point back to cleaning.wav) -> 4 FAILs.
