# SB-M33-007 — Completion SFX (V02: unchanged WON-only + Retry tail cleanup)

Status: IMPLEMENTED (AWAITING_AUDIT) — owner decision V02 §3.
Implementation SHA: `e4ccd7f`

`completion.wav` kept, WON only, once per attempt. V02 criterion 7: `reset_for_new_attempt()` now stops and releases every CLEANING and COMPLETION voice (V01 released completion only).

Evidence: `controller_unit` (LOST/ERROR silent, WON once), `retry_cleanup_unit` (3 cleaning + 1 completion active -> Retry -> no player playing, 0 active, latch re-armed, fresh WON plays), `real_stack_1x` (Retry leaves no stale voice, fresh WON completion == 2). Probe `retrytail` (drop cleaning release) -> 3 FAILs.
