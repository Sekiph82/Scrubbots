# SB-M33-009 — Concurrency management (V02 retune)

Status: IMPLEMENTED (AWAITING_AUDIT), owner F6 re-listening required. Implementation SHA: `e4ccd7f`

- Cleaning cap 8 -> 3; each voice bounded to 0.24 s (V01 up to 1.86 s). Dispatch pool (4) removed. Total SFX nodes 13 -> 4.
- Deterministic suppression, no queue, fixed node count (unchanged policy).

Evidence: `controller_unit` burst (played == cap, overflow suppressed, node count fixed); `real_stack_1x/2x` clutter stress with realistic ageing: max simultaneous cleaning voices 3 at both speeds, pitch 1.0. Headless diagnostics are bound/authority evidence, not listening evidence (AUDIT_V01 note A).
