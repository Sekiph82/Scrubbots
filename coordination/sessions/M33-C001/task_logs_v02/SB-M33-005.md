# SB-M33-005 — Dispatch SFX (V02: removed)

Status: IMPLEMENTED (AWAITING_AUDIT) — owner decision V02 §1: dispatch SFX runtime behavior NONE.
Implementation SHA: `e4ccd7f`

## Change
- `production_gameplay_host.gd`: removed `_dispatcher.assignment_dispatched.connect(_audio._on_assignment_dispatched)`.
- `gameplay_audio_controller.gd`: removed the DISPATCH category, its 4-voice pool, `_on_assignment_dispatched` and `request_dispatch`.
- `assets/audio/sfx/dispatch.wav` preserved (now the cleaning source, see SB-M33-006).
- `scrubbot_dispatcher.gd`: signal doc updated; signal itself unchanged (observation-only).

## Direct evidence (`tests/m33_audio_runtime.gd`)
- `controller_unit`: no `_on_assignment_dispatched`/`request_dispatch`, no DISPATCH category.
- `real_stack_1x/2x`: zero audio/music/haptics callables connected to `assignment_dispatched`; total SFX requests == committed clears + 1 WON (400 + 1) while committed dispatches > 0.
- Sensitivity probe `dispatchsfx` (reconnect a dispatch -> SFX lambda in host): 4 FAILs, exit 1.
