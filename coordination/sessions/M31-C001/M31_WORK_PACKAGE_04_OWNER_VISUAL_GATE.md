# M31 Work Package 04 — Owner Visual Gate

Create/use `res://scenes/debug/m31_cleaning_fx_playtest.tscn`.

Owner gate must cover:
- correct cell location;
- cleaning readability;
- normal-density visual restraint;
- high-density behavior;
- FX toggle;
- reduced mode if present;
- Retry cleanup;
- 1x/2x visual acceptability.

Claude does not self-close M31. Handoff with `CLAUDE_LOG_V01.md` and `AWAITING_AUDIT`.
