# SB-M37-005 — Target curve / micro modifiers

Status: IMPLEMENTED
Requirement: use the exact owner-locked V1 formula/config; verify representative
levels and boss→recovery drops; no silent coefficient edits.

## Production owner
`LevelProgressionService.target_challenge_for(n)` / `describe(n)` delegate to
`DifficultyProgressionV1` (M36), which reads `level_progression_v1.json`.
No coefficients are duplicated in M37.

## Direct tests
`_boundaries` — 311 EASY target > 11 EASY target (campaign-age growth).
The full target regression + recovery drops are covered by
`tests/m36_difficulty_v1.gd` (levels 1..1010, boss->recovery >=35/20/15) which
M37 reuses rather than re-implementing.

## Failure condition
Target curve diverges from the locked formula, or a coefficient is edited
outside the versioned config.

## Actual
PASS (M37 + M36 tests).
