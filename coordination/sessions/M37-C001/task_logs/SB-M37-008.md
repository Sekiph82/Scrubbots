# SB-M37-008 — Tests

Status: TESTED
Requirement: direct tests for cadence, current level, first-clear, duplicate
callback, replay, reset/reload snapshot, large level numbers, invalid inputs,
and M36 target calculations.

## Deliverable
`tests/m37_level_progression.gd` covers the full critical adversarial matrix:
- cadence + large n;
- current level + frontier advance + invalid set;
- first-clear idempotency + reentrant burst;
- replay win/loss non-advancing;
- stale completion after frontier advance;
- invalid level numbers (0, negative);
- cadence boundaries 10->11, 20->21, 310->311;
- snapshot round-trip;
- malformed snapshot fail-closed (null / wrong schema / current<1 / non-int);
- no content mutation (fresh read model per describe()).

## Result
```
godot --headless --path . -s res://tests/m37_level_progression.gd
```
**PASS** (all assertions).

## Failure condition
Any adversarial case advancing progression incorrectly or corrupting state.
