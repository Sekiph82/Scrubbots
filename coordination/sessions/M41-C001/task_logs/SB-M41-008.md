# SB-M41-008 — Relaunch tests

Status: IMPLEMENTED (AWAITING_AUDIT). Implementation SHA: `8a423d3`

- `exact_relaunch`: fresh AppState on the same save restores exact snapshot {0.35, 0.2, 0.85, on, OFF, on} + Haptics OFF and re-applies buses (buses neutralized in between); second relaunch after re-enabling restores exactly again.
- `main_root_settings`: real `main.tscn` root -> SETTINGS -> Music OFF + SFX slider 0.45 (not drag-ended) -> lifecycle flush `focus_out` -> free -> new root on same save restores Music OFF + SFX 0.45 and the UI shows "45%".
- `deferred_slider_persistence`: pending drag value not on disk before flush, on disk after.
