# SB-M41-006 — Persistence

Status: IMPLEMENTED (AWAITING_AUDIT). Implementation SHA: `8a423d3`

- Single authority: canonical M40 `SaveService` save (`user://scrubbots_save.dat`). `collect()` writes `settings.audio = AudioSettingsService.snapshot()` = {master, music, sfx, master_on, music_on, sfx_on}; haptics unchanged (`settings.haptics.enabled`).
- Validation: toggles optional (pre-M41 saves => ON); present non-bool => whole candidate rejected (`audio_<k>_type`, AL-083). `_apply` and its exact-restore path apply toggles. Schema VERSION unchanged (additive optional keys).
- Save timing: toggles/haptics save immediately; slider drags apply live + `mark_dirty`, saved on drag end, Close (`flush_if_dirty`) and the existing app lifecycle flush (pause / focus-out / close request).
- No write to legacy `user://audio_settings.cfg` / `haptics_settings.cfg`.

Evidence: `exact_relaunch`, `deferred_slider_persistence`, `toggle_schema_validation` (21/21 non-bool values rejected; exact bool false valid; corrupt-toggle primary rejected wholesale; pre-M41 primary loads ON), `blocked_app_readonly`, `no_legacy_side_files`. Probes `togglestrict` (2 FAILs), `togglepersist` (3 FAILs).
