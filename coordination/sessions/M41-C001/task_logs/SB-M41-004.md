# SB-M41-004 — Haptics

Status: IMPLEMENTED (AWAITING_AUDIT) / SETTINGS_OWNER_F6_REQUIRED (real-device feel stays M34 SB-M34-006)
Implementation SHA: `8a423d3`

- UI "VIBRATION" toggle -> `AppState.set_haptics_enabled(on)` (existing M40 action; persists).
- Live apply: `HapticsController.bind_settings(app_state.haptics)` in the production host; `is_enabled()` reads the canonical setting at every request, so a Settings change applies without rebuild. A malformed settings answer fails closed to no vibration.
- Haptics OFF suppresses vibration only — no gameplay read/write.

Evidence: `haptics_live_no_gameplay_mutation` — two real-host drains to WON with the toggle flipped AFTER build: ON 141 platform calls, OFF 0 calls, both WON with 400 clears (identical gameplay); malformed-answer double -> disabled. Probe `hapticlive` (revert to build-time copy) -> 2 FAILs.
