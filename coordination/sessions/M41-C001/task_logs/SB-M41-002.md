# SB-M41-002 — Music

Status: IMPLEMENTED (AWAITING_AUDIT) / SETTINGS_OWNER_F6_REQUIRED
Implementation SHA: `8a423d3`

- Canonical owner: `AudioSettingsService` inside the ONE `AppState` (`app_state.audio`); UI calls `AppState.set_audio_volume("music", v, persist)` / `set_audio_enabled("music", on)`.
- Slider 0..100 % (step 5 %), ON/OFF toggle (OFF mutes the bus and keeps the level; ON restores it), value label ("NN%" / "OFF").
- Live apply: AudioServer bus volume/mute changes immediately.
- Isolation: Music 0 / Music OFF mutes only the Music bus (SFX and Master unmuted); live during real gameplay.

Evidence (`tests/m41_settings.gd`): `service_toggle_semantics`, `live_apply_actions`, `bus_isolation_via_appstate`, `settings_ui_controls`, `real_host_bus_routing`, `exact_relaunch`; bus-level also in `tests/m33_audio_runtime.gd::bus_routing_independence`.
