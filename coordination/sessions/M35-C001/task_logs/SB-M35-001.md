# SB-M35-001 — Production LevelCatalog

Status: IMPLEMENTED
Requirement: one explicit declarative production catalog authority; no
runtime broad directory scan that could accidentally include test fixtures.

## Production owner
`scripts/data/level_catalog.gd` reading `data/levels/catalog/production_catalog_v1.json`.

## Direct tests
`tests/m35_level_catalog.gd::_production_catalog_loads`:
- catalog loads (`ok`);
- exactly 1 production entry (M21 hazard-bot);
- entry surfaced with M21 id/dims/difficulty;
- batch revalidation re-runs `ok`.

## Failure condition
Manifest missing / malformed / wrong schema id -> fail closed with error.

## Actual
PASS.
