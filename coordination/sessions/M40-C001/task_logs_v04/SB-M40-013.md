# SB-M40-013 — V04 remediation (Wall-clock timestamps / duplicate claims)

Status: IMPLEMENTED (AWAITING_AUDIT)
V03 audit: coordination/sessions/M40-C001/CHATGPT_AUDIT_V03.md (frozen F-M40-V03-001..004)
V04 prompt/criteria: coordination/sessions/M40-C001/CHATGPT_PROMPT_V04.md / CHATGPT_AUDIT_CRITERIA_V04.md
Depends on: M39 V04 implementation 3a8943d (local calendar + ProductionActionFacade)
Finding: F-M40-V03-004

## Remediation
End-to-end Daily through the real root using the M39 V04 local-calendar provider (seams injected via `main.gd` static boot overrides; production root uses the OS local calendar). Lifecycle flush (`NOTIFICATION_APPLICATION_PAUSED`, `NOTIFICATION_APPLICATION_FOCUS_OUT`, `NOTIFICATION_WM_CLOSE_REQUEST`) -> `AppState.flush()`; blocked app never writes.

## Direct test
tests/m40_v04_bootstrap.gd::_daily_local_boundary_relaunch (same local day across UTC new year cannot regrant; next local day claims; rollback blocked; month boundary consecutive; production root provider == OS local date), ::_lifecycle_flush_relaunch.

## Result
PASS (see CLAUDE_LOG_V04.md regression table).
