# SB-M38-016 — V03 strict-evidence repair

Status: IMPLEMENTED (AWAITING_AUDIT)
Re-audit: coordination/sessions/M38-C001/CHATGPT_FULL_SURFACE_REAUDIT_V02.md (frozen F-M38-REOPEN-001..002)
V03 prompt/criteria: coordination/sessions/M38-C001/CHATGPT_PROMPT_V03.md / CHATGPT_AUDIT_CRITERIA_V03.md

## Pre-fix evidence
`tests/m38_v02_strict.gd` (unchanged) printed at `_reward_failure_no_mutation`:
`SCRIPT ERROR: Invalid type in function 'new' in base 'GDScript'. The Object-derived class of argument 1 (RefCounted (_FakeReward)) is not a subclass of the expected argument class.`
and still ended `M38 V02 strict validation: PASS`, exit 0.

## Change (test/evidence only)
- New `tests/support/failing_reward_grant_service.gd`: extends the real
  `res://scripts/economy/reward_grant_service.gd`, `super(wallet)`, overrides
  `grant()`/`already_applied()` to return false, counts calls.
- `_reward_failure_no_mutation` uses it; asserts `is RewardGrantService`, inherited
  `wallet()` is the test wallet, WinStreakService accepted it, grant/already_applied
  were really called, `applied=false / reason=reward_failed`, streak/SB/Bot Parts/Gift
  total+cycle unchanged, level 1 not in `processed`, a healthy service importing that
  state still wins level 1; sentinel counter at the final assertion.
- Expected/completed ledger (`EXPECTED_CASES`, `_complete()` as each case's last
  line, `_missing_cases()`), checked in `_done()` -> missing case = FAIL, exit 1.
- `_ledger_sensitivity`: synthetic missing `case_b` detected; none missing when all complete.
- Removed the non-subclass `_FakeReward`. WinStreakService typing untouched.

## Validation
Unchanged production: 11/11 cases complete, 0 SCRIPT ERROR, PASS, exit 0 -> no
production defect exposed. Live abort probe (temp copy with a non-subclass fake,
deleted after): SCRIPT ERROR, `sub-test did not complete: reward_failure_no_mutation`,
10/11, FAIL, exit 1.

Production M38 files changed: NONE.
