# SB-M38-012 — Persistence

Status: IMPLEMENTED (durable file finalized by M40)
Requirement: versioned deterministic snapshot/import contract. Durable central
file persistence finalized by M40. M38 must not create a competing save
authority.

## Production owner
- `WinStreakService.snapshot()/import_snapshot()` — schema
  `scrubbots.winstreak.v1`, {streak, processed[]}.
- `RewardGrantService.snapshot()/import_snapshot()` — applied tx ids + wallet.
- `GiftMeterService.snapshot()/import_snapshot()` — cycle progress/total/queue.
- `EconomyWallet.snapshot()/import_snapshot()`.

No M38 code writes a save file; M40 SaveService aggregates these snapshots.

## Direct tests
`_snapshot_import` — round-trip; duplicate replay after import does not re-grant.
`_malformed_snapshot` — fail-closed, state untouched.

## Actual
PASS. Dependency: M40 wires these into the durable save.
