# SB-M38-003 — Reset semantics

Status: IMPLEMENTED
Requirement: progression loss resets to 0; restart after real gameplay began
resets to 0; pre-action exit does not reset; replay loss does not mutate streak.

## Production owner
`WinStreakService`:
- `on_progression_loss()` -> streak 0 always;
- `on_gameplay_started()` arms the restart reset;
- `on_restart()` resets only if gameplay had begun;
- `on_pre_action_exit()` resets nothing.

## Direct tests
`_reset_semantics` — loss resets; restart-after-gameplay resets; pre-action
exit keeps streak. `_replay_isolation` covers replay non-mutation.

## Actual
PASS.
