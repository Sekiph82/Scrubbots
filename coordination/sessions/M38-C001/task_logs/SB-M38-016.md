# SB-M38-016 — Integration tests

Status: TESTED
Requirement: cover reset, replay, duplicate/reentry, stale callbacks, snapshot/
import, progression transition, and reward idempotency.

## Deliverable
`tests/m38_win_streak.gd` — full adversarial matrix:
- WON twice / first-clear twice / reentrant burst;
- retry/restart ordering + loss reset + pre-action exit;
- stale old-level WON after advancing;
- replay win/loss isolation;
- streak transitions 4->5, 5->6, 9->10 (bot part crossings);
- snapshot/import then duplicate transaction replay (no re-grant);
- malformed negative/huge snapshot fail-closed;
- RewardGrantService unknown-resource fail-closed (no partial advance);
- GiftMeterService source isolation + rollover atomicity.

## Result
```
godot --headless --path . -s res://tests/m38_win_streak.gd
```
**PASS** (all assertions). Exit 0.

## Actual
PASS.
