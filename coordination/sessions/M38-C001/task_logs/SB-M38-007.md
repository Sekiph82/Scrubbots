# SB-M38-007 — Mapping test (active streak 3 -> +10 SB)

Status: TESTED
Requirement: streak SB mapping is based on the active streak AFTER increment; verify active streak 3 -> +10 SB.

## Production owner
`WinStreakService.streak_sb_for(active_streak)` (owner-locked 1/5/10/25/100),
applied inside `process_first_clear_win` after the streak increments.

## Direct tests
`tests/m38_win_streak.gd::_mapping` asserts the full table (1,2,3,4,5,6,50,0)
and `_reward_path_and_wallet` proves the mapping is applied to the wallet
through RewardGrantService for a real win sequence.

## Expected / failure
Expected: active streak 3 -> +10 SB. Failure: wrong SB for the active streak value.

## Actual
PASS.
