# SB-M38-014 — Bot Part multiple-of-5

Status: IMPLEMENTED
Requirement: grant exactly +1 Bot Part when the active streak reaches
5,10,15,..., once per threshold crossing. No grant at 0 or non-multiples; no
duplicate on callback replay.

## Production owner
`WinStreakService.process_first_clear_win`: after increment, if
`_streak % 5 == 0`, grant `{bot_parts: 1}` via `reward.grant("streak_bp5:L<n>")`.
The per-level tx id makes the crossing idempotent.

## Direct tests
`_bot_part_multiples_of_5` — wins 1..10 grant 2 bot parts (at streak 5 and 10);
streak 4 grants none.

## Actual
PASS.
