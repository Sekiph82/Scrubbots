# SB-M38-001 — Streak state

Status: IMPLEMENTED
Requirement: one authoritative WinStreakService owns the active progression
streak; state non-negative, snapshot/import safe, not directly mutable by UI.

## Production owner
`scripts/economy/win_streak_service.gd`. `_streak` is private; only
`process_first_clear_win` / reset methods change it. `streak()` is read-only.
Snapshot rejects negative streak.

## Direct tests
`tests/m38_win_streak.gd::_snapshot_import`, `_malformed_snapshot` — negative
streak rejected, state untouched on bad import.

## Actual
PASS.
