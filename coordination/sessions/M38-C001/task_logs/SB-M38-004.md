# SB-M38-004 — RewardGrantService path

Status: IMPLEMENTED
Requirement: exact streak SB mapping (1/5/10/25/100) granted through
`RewardGrantService`, never direct wallet mutation. If the full M39 service
doesn't exist, create the narrow canonical foundation at the final M39 path.

## Production owner
- `scripts/economy/reward_grant_service.gd` (final M39 path) — canonical grant
  authority, idempotent + atomic, default handlers for scrub_bucks/bot_parts.
- `scripts/economy/economy_wallet.gd` — narrow wallet foundation.
- `WinStreakService.process_first_clear_win` computes `streak_sb_for(streak)`
  and grants via `reward.grant("streak_sb:L<n>", {scrub_bucks: sb})`. It never
  calls `wallet.credit` directly.

M39 will EXTEND this same service (register more resource handlers), not
duplicate it.

## Direct tests
`_reward_path_and_wallet` — wins 1..4 credit 1+5+10+25 = 41 SB through the grant
service.

## Actual
PASS.
